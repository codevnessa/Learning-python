# Usando uma função lambda para dobrar um número
dobrar = lambda x: x * 2

# Usando a função lambda
resultado = dobrar(5)
print(resultado)

"""
1. Uma função lambda é definida para receber um argumento `x` e retornar `x * 2`.
2. A função lambda é atribuída à variável `dobrar`.
3. A função lambda é chamada com o argumento 5, retornando 10.
4. O resultado é impresso, mostrando 10.

Isso mostra como funções lambda podem ser usadas para criar funções simples de forma rápida e concisa.
"""